# Real-Time Data Ingestion Dependencies
# ========================================
#
# Dependencias optimizadas para procesamiento en tiempo real
# Incluye librerías para IoT, GPS, WebSockets y cloud storage
#
# TODO (@A): Ajustar versiones según necesidades específicas

# ========================================
# CORE DEPENDENCIES
# ========================================

# HTTP and async processing
aiohttp==3.9.1
aiofiles==23.2.1
asyncio-mqtt==0.16.1
websockets==12.0

# Data processing
pandas==2.1.4
numpy==1.25.2
pytz==2023.3
python-dateutil==2.8.2

# JSON and data validation
pydantic==2.5.2
jsonschema==4.20.0
marshmallow==3.20.2

# Logging and monitoring
structlog==23.2.0
prometheus-client==0.19.0

# ========================================
# CLOUD STORAGE
# ========================================

# AWS S3
boto3==1.34.0
botocore==1.34.0

# Google Cloud Storage
google-cloud-storage==2.10.0
google-auth==2.25.2

# Azure Blob Storage
azure-storage-blob==12.19.0
azure-identity==1.15.0

# ========================================
# MESSAGE QUEUES & STREAMING
# ========================================

# Redis for buffering
redis==5.0.1
aioredis==2.0.1

# Apache Kafka (optional)
kafka-python==2.0.2
# aiokafka==0.9.0  # Uncomment if using async Kafka

# RabbitMQ (optional)
# pika==1.3.2  # Uncomment if using RabbitMQ

# ========================================
# IOT & GPS TRACKING
# ========================================

# MQTT for IoT devices
paho-mqtt==1.6.1

# GPS and geolocation
geopy==2.4.1
pyproj==3.6.1

# Serial communication (for direct IoT)
pyserial==3.5

# ========================================
# HTTP CLIENT & APIs
# ========================================

# HTTP clients
requests==2.31.0
httpx==0.25.2

# Rate limiting
ratelimit==2.2.1
backoff==2.2.1

# ========================================
# CONFIGURATION & ENVIRONMENT
# ========================================

# Environment variables
python-decouple==3.8
python-dotenv==1.0.0

# Configuration management
pyyaml==6.0.1
toml==0.10.2

# ========================================
# TESTING & DEVELOPMENT
# ========================================

# Testing (only for development)
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0
pytest-mock==3.12.0

# Code quality
black==23.12.0
flake8==6.1.0
mypy==1.8.0

# ========================================
# PRODUCTION DEPENDENCIES
# ========================================

# WSGI/ASGI servers
uvicorn==0.24.0
gunicorn==21.2.0

# Health checks and monitoring
psutil==5.9.6

# ========================================
# OPTIONAL DEPENDENCIES
# ========================================

# Database connectivity (if needed)
# asyncpg==0.29.0  # PostgreSQL async
# aiomysql==0.2.0  # MySQL async
# motor==3.3.2     # MongoDB async

# Encryption and security
# cryptography==41.0.8
# bcrypt==4.1.2

# Image processing (if handling camera data)
# Pillow==10.1.0
# opencv-python==4.8.1.78

# Scientific computing (if advanced analytics)
# scipy==1.11.4
# scikit-learn==1.3.2

# ========================================
# VERSION PINNING NOTES
# ========================================

# All versions are pinned for reproducible builds
# Update carefully and test in development first
# 
# To update dependencies:
# 1. pip-compile requirements.in  # if using pip-tools
# 2. pip install -r requirements.txt
# 3. Run tests to ensure compatibility
#
# For security updates:
# pip-audit  # Check for known vulnerabilities
